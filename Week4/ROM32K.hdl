ROM32K.hdl

CHIP ROM32K {
    IN address[15];
    OUT out[16];

    PARTS:
    ROM16K(address=address[0..13], out=out1);
    ROM16K(address=address[0..13], out=out2);

    Mux16(
        a=out1,
        b=out2,
        sel=address[14],
        out=out
    );
}