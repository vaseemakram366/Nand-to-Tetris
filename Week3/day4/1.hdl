// XOR HDL Implementation

CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    Nand(a=a, b=a, out=x);
    Nand(a=b, b=b, out=y);
    Nand(a=a, b=y, out=z);
    Nand(a=b, b=x, out=z1);
    Nand(a=z, b=z1, out=out);
    //// Replace this comment with your code.
}